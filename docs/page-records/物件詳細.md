# 物件詳細ページ

物件1件の情報を見せて、内見予約・問い合わせにつなげるページ。実装順 2(2026-09-09〜09-14)。

このサイトは制作の記録を残すための架空のサイト。会社名・住所・電話番号・免許番号・人名・物件はすべて架空で、番地は `0-0-0`、電話は `03-0000-0000`、免許番号は `000000` に統一している。

判断の経緯(なぜその形にしたか)は `docs/記録シート.md` の J-037〜J-079 にある。ここでは**何ができるか**だけを書く。

---

## 画面の構成(上から順に)

| # | 要素 | 内容 |
|---|---|---|
| 1 | パンくず | トップ > 賃貸(売買)物件を探す > 物件名 |
| 2 | 写真ギャラリー | メイン画像+サムネイル。間取り図は最後 |
| 3 | 右カラム | 状態バッジ・種目 → 物件名 → 駅徒歩+町名 → 価格 → 費用の内訳 → 間取り・面積・階 → 設備 → 要約4項目 → ボタン2つ |
| 4 | 物件データ | 開閉する3区分(入居前に確認すること / 建物 / 契約・掲載)。最初は「確認」だけ開く |
| 5 | この物件について | ポイントタグ最大3つ + 担当者のコメント(写真・氏名・役職・資格) |
| 6 | 所在地 | 地図(OpenStreetMap) |
| 7 | この物件を見てみる | 予約と問い合わせのボタン |
| 8 | 同じエリア・条件の物件 | カード4件 |
| 9 | 最近見た物件 | カルーセル(見た履歴がある時だけ出る) |

PC(1024px以上)は 2・3 を左右に並べ、それより狭い幅では縦に積む。スマホは画面下に固定ボタン(電話 / LINE(ダミー)/ 内見予約)。

## 機能

**写真ギャラリー**
画像を押すと全画面で開く。左右の矢印・キーボードの ← → ・スマホの横スワイプで送れ、端まで行くと先頭に戻る。閉じるのは × ・Esc・画像の外側のクリック。サムネイルからも直接選べる。ライブラリは使わず、ブラウザ標準の `<dialog>` と CSS だけで作っている。写真が0枚の物件(60件中3件)は「写真準備中」と出し、押すと間取り図だけが開く。

**物件データ**
賃貸・売買それぞれの項目を3区分に分けて表示する。開閉は JavaScript を使わず `<details>` で行い、閉じていても中身は HTML に存在する(検索エンジンから見える)。項目は種別ごとに固定で、値が無い場合は「—」を出す。取引態様・物件番号・情報更新日・次回更新予定日を必ず含む。

**一覧への回遊**
駅名・沿線・間取り・設備・町名・種目・ポイントタグを押すと、その条件で絞り込んだ物件一覧へ移動する。一覧の絞り込みに無い条件(向き・築年・面積・金額など)はリンクにしていない。

**ポイントタグ**
特集・駅徒歩・築年・設備から自動で最大3つ選んで出す。データに無い形容(閑静・人気など)は使わない。

**担当者のコメント**
物件のデータ(区・町・駅・徒歩分・築年月・構造・階数・間取り・面積・設備・費用・引渡しなど)から7文・150〜200字を組み立てている。データに無いことは書かない。写真はプレースホルダーで、実写真は使わない。

**地図**
OpenStreetMap + Leaflet。ズーム15固定・ホイール操作は無効。架空の住所なので、ピンは町の代表座標付近に置き、その旨をページに明記している。

**同じエリア・条件の物件**
同じ町 → 同じ沿線 → 同じ種別の順に4件。成約済みと自分自身は含めない。

**最近見た物件**
見た物件の番号だけをブラウザに保存(最大10件)。サーバーには送らない。PCは矢印で1件ずつ送り、スマホは横スワイプ。

**成約済みの物件**
ページを消さず「この物件は成約しました」と表示し、内見予約のボタン(画面下の固定ボタンも)を隠す。問い合わせと関連物件は残す。

## データと生成

WordPress(ローカルのDocker)で作った架空の物件60件(賃貸40・売買20)を JSON に書き出し、ビルド時に読んで**全60ページを静的なHTMLとして生成**している。公開後にサーバー側で組み立てる処理は無い。

## まだ作っていないもの

| 未実装 | いつ作るか |
|---|---|
| 予約・問い合わせフォーム(ボタンの飛び先) | 実装順 5。現在は押すと404。物件番号は URL に載せてある |
| 構造化データ・sitemap・llms.txt・OGP、そのうえで noindex | 実装順 8。現時点では未実装 |
| このページから出ていくリンク先(会社案内・初めての方へ・宅建業法表示など) | 実装順 6・7 |
| 404ページ | 未決定 |

**既知の課題**:01 設計図では初期費用として敷金・礼金・仲介手数料を出すと決めているが、**仲介手数料が画面に出ていない**(データは持っている)。

**未確認**:実機(iOS Safari / Android Chrome)での確認、スクリーンリーダーでの読み上げ、表示速度の計測は未実施(実装順 9)。キーボードだけでの操作(ギャラリー・開閉・カルーセル・ボタン)は 1280px で確認済み。
