# 03 デザイン基準(v0.56 / 2026-09-13)

**これは初案。** 色(J-028)とフッター(J-027)は決定済み。それ以外は SHIN の判断待ち。
全ページはこの1枚から派生させる。ページごとに色や余白を足さない(章2「ページごとに雰囲気が変わる」を止めるため)。
対象:スマホ 390px を先に設計し、PC 1280px に展開する。

---

## 1. トーン

- **信頼感・清潔感**。25年やっている地元の会社が「ちゃんとしている」ことを、写真ではなく **書体・余白・情報の順序** で出す
- 主役は物件検索と物件情報。装飾は入れない。イメージ写真は使わない(プレースホルダーのみ)
- 参考にしたもの:LIFULL HOME'S(検索の入口・条件の並び・カードの情報順)/ 住宅ファミリー社(フッター)。外したもの:右カラム・ランキング・記事・通知・写真主体のレイアウト
- **情報の優先順位(J-039)**:関心の高い情報は、価値の薄い情報と同列に並べない。詳細のポイントタグ(最大3・データから導出)、一覧カードの4項目のように、先に判断できる形で切り出す。タグの語は絞り込み条件と同じ語彙を使い、データにない形容(閑静・人気 等)は使わない

## 2. 色(J-028・案D)

| 役割 | 名前 | hex | 使う場所 |
|---|---|---|---|
| 主色 | 墨 | `#2B2F33` | ヘッダー文字・見出し・フッター背景・主ボタンの文字 |
| 文字 | 墨(本文) | `#1F2933` | 本文。純黒は使わない |
| 文字(弱) | 灰 | `#5F6B75` | 補足・ラベル・日付 |
| 面 | 白 | `#FFFFFF` | ページ背景・カード |
| 面(区切り) | 薄灰 | `#F3F4F5` | セクションの帯・検索パネル・表の見出し |
| 線 | 灰線 | `#D9DDE1` | カード枠・表の罫線・入力枠 |
| **強調** | 青緑 | `#1F8A8A` | **検索ボタン・主CTA(内見予約・査定依頼・相談)だけ**。リンク・見出し・バッジには使わない |
| 強調(押下) | 濃青緑 | `#176C6C` | hover / active |
| リンク | 青緑(暗) | `#176C6C` | 本文中のリンク。下線あり |

バッジ(4種・強調色と別系統):

| バッジ | 背景 | 文字 | 備考 |
|---|---|---|---|
| 新着 | `#E0F2F1` | `#176C6C` | 公開14日以内(lib/badges.ts) |
| 値下げ | `#FDECEA` | `#B3261E` | 赤系。強調色と競合しない |
| 商談中 | `#EEF0F2` | `#5F6B75` | 灰 |
| 成約済み | `#3A3F45` | `#FFFFFF` | 濃灰。カード全体を 60% に落とす。成約済みは単独表示(新着・値下げを併記しない・J-038) |

プレースホルダー画像:単色は **`#3A3F45` 〜 `#9AA3AB` の墨系5段階**に揃える(seed の `$PH_COLORS` を変更 → J-ID)。写真の代わりに色で暴れさせない。

コントラスト:本文 `#1F2933` on `#FFFFFF` = 15:1、青緑ボタンの白文字 = 4.6:1(AA 相当)。薄灰の上の灰文字は 4.5:1 を下回るので、ラベル以外に使わない。

## 3. 書体

| 用途 | 書体 | 太さ | 備考 |
|---|---|---|---|
| 全体 | Noto Sans JP | 400 / 500 / 700 | 1種のみ。見出しも同じ書体で太さと大きさだけ変える |
| 数字(家賃・価格・件数) | 同 | 700 | `font-variant-numeric: tabular-nums` で桁を揃える |

行間:本文 1.7 / 見出し 1.3 / 表 1.5。字間:見出しのみ -0.01em。

## 4. サイズ(スマホ → PC)

| 役割 | スマホ | PC | 用途 |
|---|---|---|---|
| Display | 28px / 700 | 40px | トップのキャッチだけ |
| H1 | 24px / 700 | 32px | ページ見出し・物件名 |
| H2 | 20px / 700 | 24px | セクション見出し |
| H3 | 17px / 700 | 18px | カード見出し・表の見出し |
| 本文 | 15px / 400 | 16px | |
| 小 | 13px / 400 | 13px | ラベル・補足・日付 |
| 最小 | 11px | 12px | バッジ・架空注記バー |
| **価格(カード)** | 20px / 700 | 22px | カードで最も大きい要素 |
| **価格(詳細)** | 28px / 700 | 36px | |

## 5. 余白と幅

余白スケール(これ以外の値を使わない):**4 / 8 / 12 / 16 / 24 / 32 / 48 / 64px**

| 場所 | スマホ | PC |
|---|---|---|
| 画面の左右余白 | 16 | 32 |
| 本文の最大幅 | — | 1280(v0.2 の 1080 から変更・J-045。左右余白 16 / 32 は不変) |
| セクション間 | 48 | 64 |
| セクション内の要素間 | 24 | 32 |
| カード内 | 12 | 16 |
| カード間 | 12 | 16 |
| 角丸 | 6px(ボタン・入力・カード共通) | 同 |
| 影 | カードは影なし・線で区切る。検索パネルだけ弱い影 `0 4px 16px rgba(43,47,51,.08)` | 同 |

## 6. 部品

**ボタン**

| 種類 | 見た目 | 用途 |
|---|---|---|
| 主 | 青緑背景・白文字・高さ 48(スマホ)/ 44(PC)・全幅(スマホ) | 検索・内見予約・査定依頼・相談 |
| 副 | 白背景・墨の1px枠・墨文字 | 詳細を見る・条件をクリア |
| 電話 | 墨背景・白文字・電話番号を表示 | ヘッダー・固定CTA。営業時間の併記は PC ヘッダーのみ(J-029) |
| リンク風 | 青緑(暗)・下線 | 「駅から探す」等の脇の導線 |

**アイコン**:lucide-react・20px・currentColor(親の文字色を継承)。線幅は既定。装飾には使わない

**絞り込み欄(左カラム・ドロワー)**:項目の文字は本文より1段小さい「小」13px、区名・沿線名の小見出しは「最小」11 / 12px。「エリア」「駅」などの見出しは H3 のまま。チェックボックスは項目文字と同じ高さ(13px)。セレクトは 13px・高さ 40(スマホ)/ 32(PC)・余白 8(チップと同寸。§6 フォーム入力の 46 は本文中のフォーム用)。絞り込み欄の部品(チップ・チェック・セレクト)は hover / active に 150ms・cubic-bezier(0.4,0,0.2,1) のトランジション(§8 の範囲内・J-035)。チップは hover で枠・文字が青緑+背景を淡い青緑 `#E0F2F1`(§2 新着バッジの背景と同じ段。選択中の濃い塗りとは区別)、チェック(ラベル含む)・セレクトは枠を一段濃く(灰)+背景を同じ淡い青緑。いずれも cursor: pointer。focus の輪郭は変えない。prefers-reduced-motion では無効(J-035)

**チップ(トグル)**:角丸 6px・高さ 40(スマホ)/ 32(PC)・13px・内側余白 8(J-035)。選択中は青緑の塗り+白文字、未選択は灰線の枠+墨文字。駅フィルターに使う(沿線ごとに横流し。複数路線の駅は config の lines 順で最初の沿線にだけ置く・重複なし・J-034)

**絞り込み欄の構成(PC 左カラム・ドロワー共通・J-034)**:常時表示 = エリア / 駅 / 家賃(価格)/ 間取り(種目)。「詳細条件」(details・初期は閉・選択中の件数を見出しに)= 駅徒歩 / 築年数 / 面積 / 設備・条件。設備は 02 §2 の順で上位6つを表示し、残りは「もっと見る」で展開。折りたたみの状態は URL に入れない。余白:左カラム内側 12(カード内と同じ段)、チップの横間隔 4・チェック項目の横間隔 12(縦は 8 のまま)。エリア・間取り・設備のチェックは横並び折り返し(J-034)

**フォーム入力**:高さ 46・枠 `#D9DDE1`・focus は青緑 2px。ラベルは上・13px・灰。必須は「必須」の文字(赤の * は使わない)。エラーは入力の下に赤文字で「何が・どう直すか」。

**物件カード(情報の順序・上から。J-046 で 4項目 → 7項目)**
1. プレースホルダー(比率 3:2)。バッジは左上に最大2つ。**左下に種目ラベル、右下に写真枚数**(カメラアイコン+「6枚」。0枚は「間取り図のみ」)。どちらも薄い墨の半透明地に白文字・最小(本物の写真に載る前提)
2. **家賃 + 管理費(小さく横に・なしは「管理費なし」)/ 価格** — 最大。売買は価格のみ
3. 間取り・面積・**階**(マンション / アパートのみ。戸建・土地は出さない)
4. **最寄1駅目**と徒歩分(2駅目は出さない)
5. 築年
6. **カテゴリータグ 最大2**(特集 > 設備・押せない・J-044)。0件でも枠の高さ(24)は残し、タグあり・なしでカードの高さをずらさない
7. 物件名 + **区・町**(墨田区曳舟)— 最小・灰
成約済みはカード全体を 60% に落とし、バッジで明示。一覧では成約済みを出し、並びは常に末尾(J-033)。
**カードは全幅で縦型1種類(画像上・文字下)。列数のみ幅で変える**:〜767px 1列 / 768〜1023px 2列 / 1024px〜 2列+左カラム(J-033・375px と 974px の実機確認で横型から変更)。

**旧仕様(J-047 の帯 → J-059 の3項目。v0.21〜v0.47。J-070 で廃止。経緯の記録)**:見出し直下に**決め手の3項目のカード**(白・角丸 6・灰線・影なし。セクションの薄灰の上に置く)。タブレット(768 以上)から3等分のグリッド、PC は1項目 約400px。セル間に細い縦線、スマホ(〜767)は1列に積み区切りは横線のみ。各セルは ラベル(最小・灰・左に lucide 20px・青緑)+ 値(**H1**・24 / 32px)+ 補足(小 13px・灰)を左揃えで上下に。値は J-063 で H2 → H1 に1段だけ上げ、3項目とも同じ大きさにする(1項目だけ大きくしない)。値は1行に収め、折り返さない。項目は種別ごとに固定(J-059):
- 賃貸 = 交通(補足に町)/ 家賃(補足に 敷2・礼1・仲1)/ 間取り・専有面積(補足に築年・J-063)
- 売買マンション = 所在地(補足に 最寄駅 徒歩分)/ 価格(補足に 管理費・修繕の月額合計)/ 間取り・専有面積(補足に築年・J-063)
- 戸建 = 所在地 / 価格 / 間取り・建物面積(補足に 土地面積)
- 土地 = 所在地 / 価格 / 土地面積(補足に 建ぺい率・容積率)
アイコン:交通 = TrainFront / 所在地 = MapPin / 家賃・価格 = JapaneseYen / 間取り・面積 = LayoutGrid / 土地面積 = Square。
重視される順(立地 → 価格 → 間取り・広さ)に合わせ、購入は毎月の支払いで判断されるため価格と月額を同じ塊に置く。管理費・修繕はここでは合計を1つ、内訳(合算しない併記)は下の情報表に残す。
誇張を避ける条件は a 種別ごとに項目固定・欠損は「—」 b 値のみ・形容は付けない、の2つを引き継ぐ。c「本文より1段だけ」は横一列に5項目を詰める前提が無くなったので外す(J-059)。

**旧仕様(J-047・v0.21〜v0.36。経緯の記録)**:見出し直下に**キー項目のカード**(白・角丸 6・灰線・影なし。セクションの薄灰の上に置く。PC は5等分のグリッドでセル間に細い縦線、高さ 80px 前後。スマホは2列×3行で区切りは横線のみ)。各セルは ラベル(最小・灰・左に lucide 16px)+ 値(H3)を左揃えで上下に。アイコン:家賃・価格 = JapaneseYen / 初期費用・管理費修繕 = Receipt / 面積 = Square / 築年 = Calendar / 最寄駅 = TrainFront / 土地権利 = FileText(J-047)。賃貸 = 家賃(管理費を小さく併記)/ 初期費用(敷2・礼1・仲1)/ 専有面積 / 築年 / 最寄駅 徒歩分。売買 = 価格 / 面積(マンション=専有、戸建=土地・建物、土地=土地)/ 築年(土地は —)/ 最寄駅 徒歩分 / 5項目目は マンション=管理費・修繕積立金(月額を合算せず「管理費 1.2万円 / 修繕 0.8万円」)、戸建・土地=土地権利(J-047)。誇張を避ける3条件:a 項目は全物件で固定・欠損は「—」 b 値のみ・形容は付けない(評価はポイントタグ) c 文字は本文より1段(H3)だけ・ラベルを必ず付ける。その下の**基本情報表**:4区分(基本 / 費用 / 建物 / 契約・掲載)の小見出し H3、区分の間は 24。**枠線・ラベル列の背景色なし。項目間は細い横線1本**。ラベルは 小 13px の灰、値は本文色・通常の太さ。ラベル列幅 112(スマホ)/ 144(PC)。PC(lg 以上)は左右2ペア×1行、スマホ・タブレットは1列。「設備」は非操作チップ(駅チップと同じ見た目・高さ 32)。取引態様・物件番号・情報更新日・次回更新予定日を必ず含む。初期費用は隠さず1行(価格直下の1行と二重表示を許容・J-038)。

**物件データ(J-060 → J-070 → J-074 → J-076)**:セクションの見出しは**「物件データ」**(J-076。概要は右カラムが担い、ここは数値と条件の置き場)。中身は**同列のアコーディオン3つ**で、見出しの階層は1つだけにする:**確認 → 建物 → 契約・掲載**(売買は 確認 → 土地・建物 → 掲載)。設備は右カラムへ移した(J-074)。**初期状態は確認だけ開き、他の2つは閉じる**(J-076)。見出しから1つ目までは 16。決め手は右カラムに集約したので、物件概要には置かない(J-070。J-047 の帯・J-059 の3項目は廃止。経緯は下の「旧仕様」に残す)。
1 **確認**:「入居前に確認すること」(売買は「購入前に確認すること」)。初期状態で開く・2列。**左 = お金、右 = 住まいの条件と時期**(J-061)。賃貸 = 管理費・共益費 / 更新料 / 駐車場 // 向き / 入居可能日、売買 = 管理費 / 修繕積立金 / 駐車場 // 向き / 引渡し(戸建・土地に無い項目は出さない)。駐車場は月額が発生する項目なのでお金側に置く。向きを残すのは日当たりが重視条件の上位に入るため。階数は内見で見る情報なので詳細へ送る。
2 **詳細**:素の `<details>` / `<summary>` のアコーディオン(J-062)。確認と合わせて3つが同列に並ぶ。賃貸 = 「建物」「契約・掲載」、売買 = 「土地・建物」「掲載」。**どちらも初期状態は閉じる。JavaScript は使わない**(トップの検索フォームと同じ理由・J-056)。1つにまとめると開いた時に 11項目が一度に出て量が減らないため、見たい方だけ開ける形にする。中に小見出しは置かない(見出しと重複するため)。
   閉じていても中身が分かるよう、**見出しの右に小さな説明**(小・灰)を添える:入居前に確認すること = 「管理費・更新料・駐車場など」(売買は マンション「管理費・修繕積立金・駐車場など」/ 戸建「駐車場・向き・引渡しなど」/ 土地「駐車場・引渡しなど」)、建物 = 「所在地・築年月・構造など」、契約・掲載 = 「契約期間・保証人・取引態様など」、土地・建物 = 「所在地・築年月・土地権利など」(土地は「所在地・土地権利・用途地域など」)、掲載 = 「取引態様・物件番号・情報更新日など」。
   2つは隙間を空けず、上下の細い横線で2行に見せる(1行 約50px)。開閉は §8 の 200ms・cubic-bezier(0.4, 0, 0.2, 1)、prefers-reduced-motion では動かさない。
**開閉する見出しと常時表示の見出しの区別(J-061)**:見出しの文字(H3・太字・墨)は同じにし、次の2点だけで区別する。
- 開閉する見出しは**左に開閉の印**(lucide の ChevronRight 20px・薄い灰。開くと 90 度回して下向き。200ms・§8 のイージング)。常時表示の見出しには印を付けない。
- 開閉する見出しは**行全体が hover で淡い青緑 `#E0F2F1`**(150ms・cursor pointer・J-035 と同じ規則)。押せる範囲は見出しから行の右端まで。
印は左の1つだけにする。右端に矢印を置くと、幅の広い画面で見出しと 1000px 以上離れ、矢印だけを押すものに見える(J-061)。

**畳んだ中身も HTML に出す**:`details` の中身はマークアップとして存在するので、構造化データとクロール可能 HTML の方針(制作計画 §7-8)に反しない。JS で後から差し込む形にはしない。
決め手の3項目に出した値(交通の1駅目・家賃・間取り・専有面積・町・初期費用 / 売買は町・価格・管理費+修繕の合計)は表から外す。2駅目と沿線は詳細情報の「建物」に残す。

**情報表の区分(J-054)**:区分はデータの種類ではなく読む人の関心順に置く。帯(J-047)の直下に「条件」の区分を1つ置き、その下に確認用の区分を続ける。
- 賃貸:A「入居の条件」(左 = 交通 / 間取り / 専有面積 / 階数 / 向き / 駐車場、右 = 家賃 / 管理費・共益費 / 初期費用 / 更新料 / 入居可能日。J-055 で入居可能日を右の末尾へ)→ B「建物と所在地」(所在地 / 築年月 / 構造)→ C「契約と掲載」(左 = 契約期間 / 保証人 / 取引態様、右 = 物件番号 / 情報更新日 / 次回更新予定日)
- 売買:A「購入の条件」(左 = 交通 / 間取り / 面積 / 階数 / 向き / 駐車場、右 = 価格 / 管理費 / 修繕積立金 / 引渡し。J-055 で引渡しを右の末尾へ)→ B「土地・建物の仕様」(所在地 / 築年月 / 構造 / 権利 / 用途地域 / 建ぺい率・容積率 / 接道)→ C「掲載情報」(取引態様 / 物件番号 / 情報更新日 / 次回更新予定日)
- 設備ブロックは A の中の最後(選ぶ条件なので確認用より上)。A は帯と同じ白い角丸カード(灰線・影なし・内側 16)に入れて先に読ませ、B・C はカードに入れず見出しを薄い文字色にする。A と B の間は 32。
- 項目は減らさない・折りたたまない(J-051 の「属性はできるだけ見えるように」を維持)。J-051 のリンクは区分が変わっても維持する。帯の5項目(J-047)は変えない。
- 沿線は独立した行を持たず、交通の値の下に小さい文字(小・薄)で添える(J-055。行は増やさない。沿線のリンクは維持)。
- 「なし」の値(管理費・共益費 / 更新料 / 駐車場 など)は太字。色は付けない(リンクの青緑と紛らわしくなるため)。保証人は値にかかわらず太字(J-055。有無そのものが判断に効く項目のため)。

**情報表の組み方(J-052)**:ラベル列は 6.5em の固定幅(「管理費・共益費」「次回更新予定日」が折り返さない最小幅)、値はその右に詰める。設備は2列の表に収まらないので表の外に出し、表の下に全幅のブロック(H3「設備」)としてチップを横に流す。面積(専有・土地・建物)は「基本」から「建物」へ移し、基本 = 所在地 / 交通 / 沿線 / 間取り(左2・右2)、建物 = 専有面積 / 階数 / 向き / 築年月 / 構造 / 駐車場(左3・右3)にする。「契約・掲載」は左右で中身を分ける:左 = 契約(契約期間 / 入居可能日 / 保証人 / 取引態様)、右 = 掲載(物件番号 / 情報更新日 / 次回更新予定日)。左右に混ぜると「次回更新予定日」が契約の更新に読めるため。

**押せるものの見た目(J-052・対象は物件詳細ページのみ。一覧・トップ・フォームは今回の対象外)**:詳細ページで押せるものは2種類に統一する。
1 **押せるチップ**(ポイントタグ・設備・種目):白地 + 青緑の細枠 + 青緑の文字(枠と文字は §2 のリンク色の段。新しい色は足さない)。ポイントタグの淡い塗りはやめる(設備と行き先が同じなので重みを揃える)。
2 **押せる文字**(沿線・間取り・所在地・交通):青緑の文字・下線なし。hover で下線を出す。
どちらも hover / active で背景を淡い青緑 `#E0F2F1`・150ms・cubic-bezier(0.4, 0, 0.2, 1)・cursor pointer(J-035 と同じ規則)。
押せないもの(向き・築年月・構造・駐車場・保証人・取引態様・契約期間・入居可能日・物件番号・情報更新日・次回更新予定日・面積・金額)は本文色の黒のまま。
**塗り = 状態、線 = 分類かつリンク**:新着・値下げなどの状態バッジは塗りのまま変えない。
**トップの面の使い分け(J-058)**:同じ面に白い角丸カードを2列続けない。会社の説明(強み3点)は枠も背景も持たせず アイコン(lucide 24px・青緑)+ 見出し H3 + 本文 の3列、リンクの入口(初めての方へ / 売却 / オーナー様の3枚)はカード(白・角丸・灰線、hover で淡い青緑)にする。役割が違うものを同じ形にしない。
**一覧の行は列を揃える(J-058)**:日付+タグ+タイトルのような行は、日付とタグに固定幅を与えてタイトルの左端を揃える(日付 7.5em・カテゴリータグ 8.5em。最長の「部屋探しのコツ」が折り返さない幅)。文字数で開始位置が動くようにしない。

**アイコンは隣の文字の大きさに合わせる(J-071)**:行の先頭に置くアイコンは、隣の文字に対して**同じくらいの高さ**に見える大きさにする。本文サイズ(15 / 16px)の行は 16px。線の太さの分だけ大きく見えるので、大きい文字に付ける場合は**文字の 7〜8 割**に落とす(数値をそのまま合わせない)。揃えは行の文字の大きさで変える(J-074):**大きい文字はベースライン**(`items-baseline`。アイコンの下端を文字のベースラインに載せる)、**本文サイズの行は中央**(`items-center`)。本文サイズでベースラインに載せるとアイコンが文字より 1〜1.5px 上に見えるため。色は青緑(accent)。文字サイズが幅で変わる行は、アイコンも一緒に変える。
**ただし詳細ページの右カラムの価格(家賃 / 価格)にはアイコンを付けない(J-072)**:大きさを合わせてもアイコンが価格より目立つため。価格は文字だけで、左端は物件名・住所・費用の内訳・要約と揃える(アイコンのある2行だけが 20 内側に入る)。

**押せるものはカーソルを変える(J-067)**:ボタン・サムネイル・矢印・閉じる(×)は `cursor: pointer`。拡大を開くメイン画像だけ `cursor: zoom-in`(押した先が拡大だと分かるため)。押せるものの上でテキスト選択が走らないよう `select-none` も併せて付ける(F-008)。

**ギャラリーのサムネイル(J-066)**:選択中は**青緑の枠 2px**。選択していないサムネイルも枠は 2px のままにし、色だけ変える(ページ = 灰線、モーダル = 白の 40%)。太さを変えないのは、選び直したときに画像の大きさが動かないようにするため。
ホバー中のサムネイルは薄くする(150ms・cubic-bezier(0.4, 0, 0.2, 1)・cursor pointer・prefers-reduced-motion では無効)。**選択中のサムネイルはホバーしても変えない**(押しても表示が変わらないため)。
地色で見え方が変わるので、**ページ(白地)は不透明度 60%**、**モーダル(墨95%の暗地)は明度 +50%** で薄くする(暗地で不透明度を下げると地に沈んで「濃くなった」ように見えるため)。枠の色はホバーでページが青緑、モーダルが白。

**最近見た物件のカルーセル(J-064 → J-065)**:横スクロールバーは出さない。md(768)以上は見出しの右に矢印(前 / 次・36px・灰枠)を置き、**1件ずつ** transform で送る(200ms・§8 のイージング。J-065。J-064 は1画面ずつだった)。1画面の枚数は 768〜1023 が2枚、1024 以上が4枚。スマホ(〜767)は scroll-snap の横スワイプで、カード幅は画面の 85%(次の1枚が見えて横に続くと分かる)。**端はループする**(J-065。末尾の次は先頭、先頭の前は末尾。向きは変えないので巻き戻して見えない)。**矢印に disabled は入れない**(J-049 で端の disabled がフォーカスを奪う不具合が出た経緯があり、ループなら構造的に起きない)。仕組みは「1画面の枚数+1枚」を並べて1枚分ずらし、並びを回して位置を戻す(カードの複製は持たない)。件数が1画面の枚数以下なら矢印を出さない。幅が変わっても左端の物件は保つ。カードは J-046 のまま。

**押せないタグは別系統(J-057)**:お知らせのカテゴリー(お知らせ / 部屋探しのコツ / 地域情報)は白地 + 灰の細枠 + 灰の文字の**押せないタグ**にする。物件の属性タグ(青緑の枠・文字でリンク)と同じ見た目にすると押せると誤解されるため、色で系統を分ける。絞り込みに繋がるタグだけが青緑。
**日付の表記(J-057)**:サイト全体で「2026年9月8日」に揃える(月日の先頭の0は落とす)。整形は lib/format.ts の dateLabel に寄せ、ページごとに別の書き方をしない。日付でない値(「即入居可」「相談」)はそのまま出す。

**詳細ページの部品(J-038)**:ギャラリーはメイン 3:2 + サムネイル高 56(スマホ)/ 64(PC)、間取り図を末尾に。写真0枚(60件中3件)はメインに間取り図を出さず、薄灰の面に「写真準備中」+ 小さく「間取り図を見る」を出す。押すとモーダルが開き、間取り図1枚だけを出す(矢印・枚数表示・サムネイル行はなし。J-049。J-038 のバッジは文言が重なるのでやめた)。ギャラリーはモーダル+スライダー(J-048):メイン画像を押すと全画面のモーダルが開き、拡大画像・左右矢印(lucide 24px)・「3 / 6」の枚数・サムネイル行(現在の1枚は青緑枠)・右上の × を出す。← → キー / Esc / 背景と余白のクリック / スマホの横スワイプで操作する。PC の送りは端でも巡回する(J-049。スマホのスワイプは端で止まる)。スマホ(〜767)はメイン画像自体も横スワイプでき、タップでモーダルも開く。開いている間は背面をスクロールさせない。ライブラリは足さず <dialog> と CSS の scroll-snap で作る。地図は高さ 256(スマホ)/ 320(PC)・ズーム 15・ホイール無効・墨の丸マーカー。CTA は 内見予約(売買は「見学を予約する」)と「問い合わせる」の2つ。**どちらの箱も2列**:右カラムは 1024 以上(右カラムが独立する幅)で左=内見予約 / 右=問い合わせ(J-075)、ページ下部は 640 以上で同じ並び(J-064)。それぞれの下の幅では縦に積む。成約済みは内見予約を出さないので残る1つが全幅になる(J-038 判断1)。問い合わせの文言は**「問い合わせる」の1語**に統一する(J-075。1024〜1080px の右カラムでは「この物件を問い合わせる」が2行になり、高さ 44 のボタンからはみ出す。物件詳細ページにいるので「この物件を」は自明)。予約側は「内見を予約する」「見学を予約する」のまま(いちばん狭い 182px のボタンでも1行に収まる)。上下2箇所で同じ文言にし、幅で文言を変えない。問い合わせの文言は1行にする。電話はヘッダー右上とスマホの固定 CTA に置き、詳細ページ本文には置かない。要約と CTA の間は 16(J-050。電話を外した分 24 から詰め、右カラムの下端をギャラリーの下端に合わせる)。状態バッジ(新着・値下げ)と同じ行に種目を出す:状態は塗りのバッジ、種目は塗りのない枠線だけのリンク(最小・太字・薄い文字色)で形を分ける。種目はその種目で絞った一覧へのリンク(J-051 の仕組み)。成約済みは内見予約を出さない(固定 CTA も)。右カラムの決め手(J-070):並びは 状態バッジ+種目 → 物件名(H1)→ **駅徒歩 + 町名**を1行に(本文・太字・左に lucide TrainFront 16px 青緑。町名は区切り「/」の後に 小・薄・細字で添える。番地は出さない。完全な住所は詳細情報の所在地に残す・J-071 → J-073)→ **家賃 / 価格**(価格の文字。アイコンは付けない・J-072)→ 費用の内訳(小・薄。賃貸 = 管理費・共益費 / 敷金 / 礼金、売買 = 管理費+修繕積立金の月額合計)→ 値下げ前(あれば)→ **スペック**(本文・太字・左に LayoutGrid 16px 青緑。間取り / 面積 / 階。土地は「土地 77㎡」)→ **設備のチップ**(J-052 の押せるチップ。見出しは置かずチップだけ。最多 10個で2段・72px。J-074 で物件概要から移した)→ 要約4行 → CTA。駅徒歩を住所より先に置くのは、探す段階で効くのが駅徒歩で、番地は後から確認する情報だから。物件名にアイコンは付けない。スペックの下の区切り線は置かず、余白 24 で要約と分ける(要約の罫線と役割が混ざるため)。
右カラムの要約(J-039 → J-068):価格の下・CTA の上に 築年 / 向き / 入居可能日(売買は引渡し)/ 最寄2駅目(13px)。PC(lg 以上)は**縦1列**で、ラベルは 6.5em の固定幅・値はその右(情報表と同じ規則・J-052)。上から拾い読みできるようにするため、左右の視線移動を作らない。各行の下に細い横線(`border-line` 1px・情報表と同じ)を引き、**最終行の下にも引く**(4項目なら4本)。情報表は最終行の下に線を引かないが、要約はこの下に CTA のボタンが続くため、最後の1本で情報の切れ目を示す(J-069)。行の高さは情報表と同じ上下 8 にし、行間(gap)は 0 にする(線と余白が二重にならないように)。文字の太さはラベル・値とも変えない。スマホ・タブレット(〜1023)は2列×2行のままで**線は引かない**(2列に線を引くと横に2本並んで表に見えるため)。「この物件について」(J-039):本文の上に「ポイント」チップ最大3。優先順は 特集(02 §2 の名前・順)> 駅徒歩5分以内・10分以内 > 新築・築5年以内 > 設備(02 §2 順、エアコン除外)。特集「駅徒歩5分」「新築・築浅」「ペット可」があれば同じ意味の段は出さない。値下げ・種目・間取り・費用系・2駅利用可は出さない。淡い青緑の面+青緑枠。各タグは同じ条件の一覧へのリンク〔collection / walk_max / built_max / feature・売買は type=sale〕、hover は J-035 と同じ(J-040)、属性リンク(J-051):情報表の 所在地(区・町)・交通(駅名)・沿線・間取り・設備チップも同じ仕組みで一覧へ飛ばす。URL は lib/links.ts の listHref(クエリ名は lib/search.ts と同じ・売買のみ type=sale)。リンクにするのは一覧の左カラム(J-034)に絞り込みがある項目だけで、向き・入居可能日・階・面積・築年・金額はリンクにしない。区の絞り込みは一覧に無いので、区リンクはその区の町を OR で並べる。0件になるリンクもリンクのままにする、本文は担当者カードの中に入れて1枚にする(J-053。見出しとポイントタグは外に残す)。カードは全幅・白地・灰枠で、PC(640 以上)は左に顔写真(プレースホルダー・3:4・幅 240)、右に「担当 氏名(架空)」+ 役職・資格 + 細い横線 + 紹介文。右のテキストに最大幅は付けない。スマホ(〜639)は上下に積み、写真は幅 160(3:4)。顔写真は物件写真と同じ作りの SVG プレースホルダー(灰地 `#6C737B` + 白 35% のシルエット)で、実写真は使わない。**シルエットは頭と肩・胴をつないだ1つの人型にする**(円と半円を離して置くと人に見えない・J-077)。**画像内の文字は「架空・プレースホルダー」の1行だけ**にし、氏名・役職は入れない(右のテキストに同じものが出るため)。画像単体で見られた時に架空だと分かる状態は、この1行で保つ(制作計画 §7-9)。

## 7. レイアウトの型

- **ヘッダー**:白。ロゴ / メニュー5 / 電話(営業時間付き)。スマホはロゴ+電話+ハンバーガー。**スマホの電話ボタンは電話番号のみ(営業時間は出さない・J-029)**。メニューはオーバーレイで開く。スクロール後は高さを詰めて固定
- **架空注記バー**:最上部。墨背景・白文字 11px・全ページ
- **FV(トップ)**:決定済み。キャッチ+検索フォーム(タブ・3項目・青緑ボタンに件数)。画像なし
- **セクション**:白と薄灰の帯を交互に。見出しは H2 左寄せ、右に「すべて見る」リンク
- **一覧**:切替点は md(768)/ lg(1024)の2段。左カラム幅 300(J-045)。〜767px:縦型1列・絞り込みはドロワー(J-030 前提)。768〜1023px:縦型2列・左カラムなし・「絞り込み」ボタン+ドロワー・固定 CTA はそのまま。1024px〜:左に絞り込み(常時)+ 右にカード2列(J-033)。左カラムはヘッダー直下に sticky、高さの上限は「画面高 − ヘッダー高 − 16」、超える分はカラム内でスクロール(細いスクロールバー・J-034)。**クイック条件タブ(J-042)**:賃貸/売買タブの下・件数の上に、詳細のポイントタグと同じ語彙の条件をチップで常時並べる。並びは 特集 / 駅徒歩(5分以内・10分以内)/ 築年(新築・築浅)/ 設備(上位6つ)。表示はデータ判定(その種別に1件以上ある特集・設備だけ)。押すと URL の条件が ON/OFF(左カラムと同じパラメータ・連動)。md 以上は薄灰の帯(角丸 6・余白 12)で囲い、区分見出しを同じ幅の列で左端に揃える(1行目:特集 / 2行目:駅徒歩 | 築年・細い区切り線 / 3行目:設備。1280px で2行に収まらなかったため設備は単独行・09/11 実測)。帯の下に件数と並び替え。スマホ(〜767px)は帯・見出しを外し、特集 / 駅徒歩 / 築年 を折り返して並べる(区分の間は 16。横スクロールは「タグが切れる」ため不採用)。設備はスマホでは出さずドロワーの設備チップに任せる。× 付きの条件タグはタブに無い条件(エリア・家賃・間取り・面積など)だけ出す。左カラムの駅徒歩・設備は売買にも出す
- **詳細**:ギャラリー → 物件名・価格・バッジ → 主CTA → 基本情報表 → コメント → 地図 → 関連4件 → 主CTA → 最近見た物件。成約済みは最上部に薄灰の面+H3「この物件は成約しました」のバナー(J-038)
- **スマホ固定CTA**:下部。3ボタン:電話 / LINE(ダミー)/ 内見予約(右端・J-030)。青緑は内見予約だけ。バーの高さ分の下余白を本文末尾に確保
- **フッター(J-027)**:墨背景・白文字。左に会社情報(2店舗・営業時間・架空注記)、サイトマップ5列(探す / サービス / 会社案内 / お知らせ / お問い合わせ)、矢印リンク2本(物件を探す / オーナー様へ)、最下段に著作・プライバシー・宅建業法表示。スマホは見出し5つのアコーディオン(details/summary・初期は閉・右端に ∨)。PC は5列。PC 右端に固定の縦タブ「物件を探す」(**墨背景・白文字・J-029**)
- **BtoB(オーナー向け)の切替**:(判断ポイント)色は変えず、見出しの文言と CTA(「管理のご相談」)だけ変える案 / 面の色を薄灰に反転する案

## 8. やらないこと

グラデーション / 影の多用 / 角丸の大小混在 / 青緑を装飾に使う / 純黒 / イメージ写真 / 2書体目

アニメーション:開閉・表示切替の 150〜200ms のトランジションは可(イージングは cubic-bezier(0.4, 0, 0.2, 1)。開閉 200ms・hover 150ms。prefers-reduced-motion では無効・J-035)。ギャラリーのモーダルの開閉も同じ 200ms・cubic-bezier(0.4, 0, 0.2, 1) の不透明度(dialog と ::backdrop。display は allow-discrete と @starting-style で両方向に効かせる)。写真送りは PC とスマホで分ける(J-049)。PC(lg 以上)は矢印・← →・サムネイルのどれでも transform のスライド 200ms・cubic-bezier(0.4, 0, 0.2, 1):次へ = 新しい画像が右から入り今の画像は左へ抜ける、前へ = その逆、サムネイルは今より後ろの番号なら右から・前なら左から。端(6 → 1 / 1 → 6)も同じ向きのまま巡回させ、巻き戻すようには見せない(画像の複製は持たない)。矢印は常時有効にし、枚数表示(n / 6)は残す。スマホ(〜767)は scroll-snap の横スワイプのままで、端では止まる。表示中の番号は状態で持ち、スクロール位置から読み直さない(J-048 で連打が端の手前で詰まったため)。prefers-reduced-motion では動きを止める。一覧の更新は 300ms(開閉の 150〜200ms とは別枠)。切り替え(条件・並び替え・タブ・ページ)のたびに全カードが現れ直す:不透明度 0→1 + 8px 上昇 + 表示順の順番ずらし 30ms × 最大8(最大遅延 210ms)。0件表示も同じ。件数の数字が変わった時は青緑 → 通常色へ 300ms。外枠の作り直しはしない。位置移動(FLIP)は入れない(J-036)。ページ遷移・スクロール連動・装飾の動きは不可(J-031)

## 9. 判断ポイント(この基準で未決のもの)

| # | 項目 | 選択肢 |
|---|---|---|
| 1 | スマホの絞り込み | ドロワー / インライン折りたたみ |
| 2 | 固定CTAの順序 | **決定(J-030)**:電話 / LINE(ダミー)/ 内見予約。右端(親指側)が内見予約 |
| 3 | 固定CTAと架空注記の重なり | **決定(J-030)**:注記は最上部のまま。固定バーの高さ分の下余白を本文末尾に確保し、フッターの注記・最下段を隠さない |
| 4 | BtoB の切替範囲 | 文言のみ / 面の反転 |
| 5 | プレースホルダーの色 | 墨系5段階に変更(seed 変更・J-ID) |
| 6 | 0件時の近隣提案の出し方 | 条件緩和の候補+13町の近隣(F-001 の対処) |
| 7 | 成約済み詳細ページ | **決定(J-037)**:「成約しました」+類似4件。404 にしない |

## 10. 手直しの型(初案は一発で決まらない前提)

1ページにつき「初案 → 判断 → 修正」が1〜3往復出る。往復の回数そのものが章2・章6の実測なので、減らすことより**記録すること**を目標にする。

**手直しは3種類。直す前にどれかを判定する。**

| 種類 | 例 | やること | 記録 |
|---|---|---|---|
| **A. 基準違反** | この基準に無い色・スケール外の余白・純黒・2書体目 | 基準を守るよう直させる。基準は変えない | AI の誤り → F-ID(気づいた手段を書く) |
| **B. 基準の不備** | 基準どおりに作ったらスマホで詰まる・読めない | **この基準を先に直し、版を上げてから**ページを直す。他ページにも効く | J-ID(条件追加)。基準の変更履歴を末尾に残す |
| **C. ページ固有の調整** | CTA の位置・見出しの文言・このページだけの並び | ページだけ直す | J-ID(却下 / 条件追加)か「そのまま採用」1行 |

B と C の区別が肝。C だと思って直したものが B(他ページでも出る)だと、後で全ページを触る。**迷ったら B 扱いで基準に書く。**

**1ページの流れ**
1. `/build-page` で初案 → before スクショ(スマホ・PC)を `docs/screenshots/` に
2. 判断ポイントと気になる点を A / B / C に分けて番号で返す
3. `/log-judgment` で記録 → 直させる → after スクショ
4. 2往復で収まらない項目は「AI に頼んでも直らない箇所」(章6)として記録し、手で直すか、置いて先に進む
5. コミット `feat: add <page> [J-NNN]`

**往復を増やさないために**
- 1往復目は構成と情報の順序だけ見る。色や 1px は2往復目
- 細かい調整を1つずつ J-ID にしない。1ページ 1〜2 件の J-ID に条件を箇条書きでまとめる
- 実装順 0(土台)で、ヘッダー・フッター・カードを実データで**わざと崩す**(長い物件名・写真0枚・成約済み・6枚)。ここで B を潰すと、以降は C だけになる
- 実装順 0〜3 は往復が多くて正常(基準の不備が出る時期)。4 以降は減る

## 11. 基準の変更履歴

| 版 | 日付 | 変更 | J-ID |
|---|---|---|---|
| v0.56 | 09/13 | §6 担当者の顔写真プレースホルダー:シルエットをつないだ人型に・画像内の文字を「架空・プレースホルダー」1行に | J-077 |
| v0.1 | 09/08 | 初案。色 J-028・フッター J-027 を反映 | — |
| v0.2 | 09/08 | §7 に追記:スマホヘッダーの電話は番号のみ / 縦タブは墨・白 / メニューはオーバーレイ / スマホのフッターは3列。§9-2・9-3 を決定に更新。フッターのスマホ表示をアコーディオンに(J-030) | J-029・J-030 |
| v0.55 | 09/13 | §6 セクション見出しを「物件データ」に。確認もアコーディオンにして3つを同列に(確認だけ初期表示で開く) | J-076 |
| v0.54 | 09/13 | §6 右カラムの CTA を 1024 以上で2列に。問い合わせの文言を「問い合わせる」に統一(上下2箇所) | J-075 |
| v0.53 | 09/13 | §6 設備のチップを物件概要から右カラム(スペックの下)へ移動。物件概要は 確認 → 詳細 の2段に | J-074 |
| v0.52 | 09/13 | §6 アイコンの揃えを文字の大きさで分ける(大きい文字はベースライン・本文サイズは中央) | J-074 |
| v0.51 | 09/13 | §6 右カラムの駅徒歩と町名を1行にまとめる(区切りは「/」・町名は小・薄・細字) | J-073 |
| v0.50 | 09/13 | §6 右カラムの価格のアイコンを廃止(規則自体は残す)。価格の左端は他の行と揃える | J-072 |
| v0.49 | 09/13 | §6 アイコンは隣の文字の大きさに合わせる(価格は文字の7〜8割・ベースライン揃え)。右カラムの住所を区・町までに | J-071 |
| v0.48 | 09/12 | §6 物件概要の決め手の3項目を廃止し、決め手を右カラムに集約(駅徒歩を先・アイコン3つ・スペックを1段大きく・区切り線を削除)。売買の面積の行を情報表に戻す | J-070 |
| v0.47 | 09/12 | §6 右カラムの要約に行ごとの横線(最終行の下にも・4本)。行の高さを上下 8・行間 0 に | J-069 |
| v0.46 | 09/12 | §6 右カラムの要約を PC で縦1列に(ラベル 6.5em の固定幅)。スマホは2列×2行のまま | J-068 |
| v0.45 | 09/12 | §6 押せるもののカーソル(ボタン類は pointer・拡大を開く画像だけ zoom-in)を追加 | J-067 |
| v0.44 | 09/12 | §6 ギャラリーのサムネイルの選択(青緑 2px)とホバー(ページは不透明度 60% / モーダルは明度 +50%)を追加 | J-066 |
| v0.43 | 09/12 | §6 最近見た物件のカルーセルを1画面送り → 1件送りに。端はループ・矢印の disabled は入れない | J-065 |
| v0.42 | 09/12 | §6 最近見た物件をカルーセルに(矢印・端で停止・幅で枚数)。詳細下部の CTA を 640 以上で2列に | J-064 |
| v0.41 | 09/12 | §6 決め手の3項目:値を H2 → H1 に1段上げ、3項目目の補足に築年を追加。3列は 768 から | J-063 |
| v0.40 | 09/12 | §6 詳細のアコーディオンを1つ → 2つに(建物 / 契約・掲載)。閉じた時の説明を見出しの右に追加 | J-062 |
| v0.39 | 09/12 | §6 開閉する見出しの区別(左の印+行全体の hover)を追加。確認の左右を 左=お金 / 右=住まいの条件と時期 に | J-061 |
| v0.38 | 09/12 | §6 物件概要を「決め手 → 設備 → 確認 → 詳細」の4段に。詳細は素の details で畳む(JS なし・中身は HTML に残す) | J-060 |
| v0.37 | 09/12 | §6 物件概要の帯(5項目)を決め手の3項目に置き換え(立地 → 価格 → 間取り・広さ。旧仕様は経緯として残す) | J-059 |
| v0.36 | 09/12 | §6 にトップの面の使い分け(強み3点は枠なし・入口はカード)と一覧の行の列揃え(日付 7.5em・タグ 8.5em)を追加 | J-058 |
| v0.35 | 09/12 | §6 に「押せないタグは別系統(お知らせのカテゴリー)」と「日付は 2026年9月8日 に統一」を追加 | J-057 |
| v0.34 | 09/12 | §6 入居可能日(売買は引渡し)を右列の末尾へ・沿線を交通の行の2段目へ・保証人は値にかかわらず太字 | J-055 |
| v0.33 | 09/12 | §6 「この物件について」の本文を担当者カードの中へ(全幅・左に顔写真 3:4・右に氏名と紹介文)。760 の制限は撤回 | J-053 |
| v0.32 | 09/12 | §6 情報表の区分を関心順に(A 条件は白カード・B/C は確認用・設備は A の中・「なし」は太字) | J-054 |
| v0.31 | 09/12 | §6 「この物件について」の本文と担当者カードを最大幅 760 に・カードを低く(イニシャル枠 40) | J-052 |
| v0.30 | 09/12 | §6 に情報表の組み方(ラベル 6.5em・設備は表の外・面積を建物へ・契約と掲載で左右を分ける)を追加 | J-052 |
| v0.29 | 09/12 | §6 に「押せるものの見た目」(押せるチップ / 押せる文字 / 押せないもの・塗り = 状態、線 = 分類)を追加。対象は詳細ページのみ | J-052 |
| v0.28 | 09/12 | §6 詳細の CTA から電話を外して2つに・問い合わせ文言を1行・要約と CTA の間を 16・状態バッジの行に種目(枠線のみのリンク) | J-050 |
| v0.27 | 09/12 | §6 情報表の 所在地・駅・沿線・間取り・設備を一覧へのリンクに(リンクにしない項目の線引きつき) | J-051 |
| v0.26 | 09/11 | §6 写真0枚の物件のメインを「間取り図+バッジ」→「写真準備中(押すと間取り図をモーダルで)」に | J-049 |
| v0.25 | 09/11 | §8 写真送りを「瞬時」→ PC は transform のスライド 200ms・端も同じ向きで巡回、スマホはスワイプのまま | J-049 |
| v0.24 | 09/11 | §6 詳細のギャラリーをモーダル+スライダーに(自作 dialog + scroll-snap)。§8 に開閉 200ms と写真送りは瞬時を追記 | J-048 |
| v0.23 | 09/11 | §6 キー項目を薄灰の帯 → 白カード(灰線・区切り線・ラベルにアイコン) | J-047 |
| v0.22 | 09/11 | §6 売買マンションの帯 5項目目を土地権利 → 管理費・修繕積立金(併記) | J-047 |
| v0.21 | 09/11 | §6 物件概要:キー項目の帯(5項目・誇張を避ける3条件)+ 表は罫線なし・ラベル背景なし・強調は帯へ | J-047 |
| v0.20 | 09/11 | §6 物件カードを 4項目 → 7項目に(画像内ラベル・管理費・階・カテゴリータグ・区町)。index.json に4列追加 | J-046 |
| v0.19 | 09/11 | §5 本文の最大幅 1080 → 1280。§7 一覧の左カラム幅 300 | J-045 |
| v0.18 | 09/11 | §7 一覧にクイック条件タブ(特集 / 駅徒歩 / 築年 / 設備)。条件タグはタブに無い条件だけ。左カラムの駅徒歩・設備を売買にも | J-042 |
| v0.17 | 09/11 | §6 ポイントタグの優先順に特集を最上位で追加(重複除外つき)。一覧の URL に collection を追加 | J-040 |
| v0.16 | 09/10 | §6 ポイントタグは一覧へのリンク。築年数セレクトに1年以内を追加 | J-040 |
| v0.15 | 09/10 | §1 に「情報の優先順位」の原則を追加(J-039 の一般化) | J-039 |
| v0.14 | 09/10 | §6 詳細:要約4項目・CTA 配置(全幅+2列)・表の4区分/2ペア/設備チップ/費用強調・ポイントチップ・担当者カード | J-039 |
| v0.13 | 09/09 | §2 成約済みバッジは単独。§6 詳細の寸法(表の見出し列幅・サムネ高・地図高)と CTA 文言。§7 成約バナー。§9-7 を決定に | J-038 |
| v0.12 | 09/09 | §6 絞り込み欄の構成(常時表示4+詳細条件)・駅チップの重複なし・設備は上位6+もっと見る | J-034 |
| v0.11 | 09/09 | §8 一覧の更新を「新規カードのみ」→「切り替えのたびに全カードが現れ直す」に(300ms・上昇・順番ずらしは維持) | J-036 |
| v0.10 | 09/09 | §8 一覧の更新を 300ms・8px 上昇・順番ずらし 30ms×最大8・件数の色に(150ms フェードは却下) | J-036 |
| v0.9 | 09/09 | §8 一覧の更新を「新規カードのみフェード」に修正(外枠の作り直しは却下) | J-036 |
| v0.8 | 09/09 | §8 一覧の更新はフェード 150ms(FLIP なし) | J-036 |
| v0.7 | 09/09 | §6 絞り込み欄の文字を本文より1段小さく(項目 13 / 小見出し 11・12)。チップの余白 8。チェック 13px・セレクト 40/32。部品の hover / active トランジション 150ms。§8 にイージングを追記 | J-035 |
| v0.6 | 09/09 | §6 チップ(駅フィルター)を追加。§7 一覧の左カラムを sticky+内部スクロールと明記 | J-034 |
| v0.5 | 09/08 | §6 カードを縦型1種類に(横型を撤回)。§7 一覧の切替点を md / lg の2段と明記 | J-033 |
| v0.4 | 09/08 | §6 物件カード:管理費・階は詳細のみ / スマホは横型 / 成約済みは末尾 | J-033 |
| v0.3 | 09/08 | §6 にアイコン(lucide-react・20px・currentColor)を追加。§8 のアニメーションを「開閉・表示切替の 150〜200ms は可」に | J-031 |

---
記録:色 J-028 / フッター J-027 / 参考サイト(HOME'S・住宅ファミリー社)は §1 に記載。この基準を採用したら「そのまま採用」1行、変えた項目ごとに J-ID。
