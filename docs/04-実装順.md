# 04 実装順(v0.6 / 2026-09-16)

原則:**共通部品を先に**(後で全ページが変わらないように)/ **データを使うページを先に**(型と JSON の不備を早く見つける)/ **同じ部品を使うページはまとめて**。
各段の完了条件は共通:スマホ 390px で崩れない / `DATA_SOURCE=static pnpm build` で全ルート Static / 判断ポイントに J-ID か「そのまま採用」/ before・after のスクショ。
手直しの扱いは `docs/03-デザイン基準.md` §10。

| 順 | 状態 | 作るもの | 先に要るもの | ここで確定する部品 | 章 | 目安 |
|---|---|---|---|---|---|---|
| 0 | **済**(09/08) | **土台**:03 のトークンを Tailwind 設定に / ヘッダー / フッター(J-027)/ 架空注記バー / スマホ固定CTA / ページ枠 | 03 の採否 | 全ページの外枠 | 2 | 3h |
| 1 | **済**(09/08〜09) | **物件カード + 物件一覧(賃貸)**:カード・バッジ・件数・並び替え・絞り込み(スマホ先行)・URL 同期 | 0・data 層 | カード、バッジ、絞り込みUI、URL 同期 | 1・2 | 6h |
| 2 | **済**(09/10〜13) | **物件詳細**:ギャラリー・価格・CTA・基本情報表・コメント・地図・関連4件・最近見た物件 | 1・地図ライブラリの選択(J-ID) | 表、地図、固定CTA の実動 | 1 | 6h |
| 3 | **済**(09/12〜13) | **トップ**:FV(決定済み)・探し方の入口・新着(カード再利用)・強み3点・3カード・お客様の声・お知らせ・店舗 | 0・1 | 検索フォーム → 一覧への遷移 | 1・2 | 4h |
| 4 | **済**(09/15) | **売買一覧 + 条件固定の一覧**(エリア13 / 沿線6 / 駅13 / 特集6)+ 入口3ページ(/area /line /feature) | 1 | generateStaticParams、0件画面(F-001 の対処) | 4 | 3h |
| 5 | **着手中**(09/15〜) | **フォーム3本**(内見予約 → 査定依頼 → オーナー相談。1本ずつ・別実装)。**詳細ページの ld+json に potentialAction を追加(J-083 で保留)** | 2(内見は物件を引き継ぐ)・Resend の設定 | 入力→確認→完了、送信テスト、迷惑メール判定 | 3 | 9h |
| 6 | 未着手 | **会社案内系**:会社概要・スタッフ・店舗案内・初めての方へ・売却・オーナー(BtoB 切替)・お客様の声 | 0 | 文言(架空)、BtoB のトーン | 2 | 6h |
| 7 | 未着手 | **お知らせ**(一覧・詳細)・プライバシーポリシー・宅建業法表示 | 0 | 静的ページの型 | — | 2h |
| 8 | 未着手 | **SEO / AI 検索対応**:構造化データ(Organization / RealEstateListing / FAQ / Breadcrumb)・sitemap・llms.txt・OGP → その上で **noindex**(J-011)。**詳細ページ分(RealEstateListing / BreadcrumbList)は実装順 2 で先行実装済み(J-083)。サイト全体としての整合の検証はこの段で行う** | 全ページ | — | 4 | 3h |
| 9 | 未着手 | **公開**:Vercel・環境変数・Lighthouse 前後・実機確認(端末数を記録) | 8 | 実測 | 4 | 4h |
| 10 | 未着手 | **記録ページ**(`/record`)・ルートの `README.md` | 記録シート | — | 6 | 3h |

目安の合計:約 49h(AI の見積もり。実測で置き換える)。累計 16h(09/08 時点)と足して約 65h。上限 100h・目標 80h の内側。
実働の累計は `docs/progress/time-log.md`(09/12 時点で 38.5h)。

## 各段の到達点(2026-09-13 時点)

| 順 | どこまで終わったか | 残り |
|---|---|---|
| 0 | トークン・ヘッダー・フッター・架空注記バー・スマホ固定CTA・ページ枠(J-027〜J-032) | — |
| 1 | カード・バッジ・件数・並び替え・絞り込み・URL 同期・クイック条件タブ・カードの情報量(J-033〜J-036・J-040〜J-046) | 地図モードの一覧(01 §2-4)は未実装。判断も未 |
| 2 | ギャラリー(モーダル+スライダー)・右カラムの決め手・物件データ(アコーディオン3つ)・担当者コメント・地図・関連4件・最近見た物件(J-037〜J-039・J-047〜J-076) | — |
| 3 | FV(検索フォーム)・探し方の入口3導線・新着8件・強み3点・3枚のカード・お客様の声3件・お知らせ3件・店舗案内2店(J-056〜J-058) | 飛び先の未作成ページが 404(記録シート §5 の宿題に登録) |
| 4 | 売買一覧は /properties の種別タブのまま(別ルートにしない)。条件固定の一覧 38 ページ(エリア13 / 沿線6 / 駅13 / 特集6)と入口3ページ(/area /line /feature)。固定条件の左カラム表示・緩和からの除外・入口の件数(J-095・J-096) | 一覧の地図モード(01 §2-4)は未実装のまま。緩和順序は J-033 の順(判断5 の指示と間取り・家賃の順が逆・未決) |
| 5 | 1本目 /contact(入力・確認・完了・Server Action・メール2本)+ /privacy + potentialAction(J-102・09/15)。/contact を2カラムに・/privacy を中央に(J-103・09/16) | 2本目 /sell・3本目 /owner(別の指示)。残り3件:**Turnstile の実表示と検証**(キー設定後)/ **Resend の到達・不達・迷惑メール判定(SPF / DKIM)**(キー設定後)/ **next.config.ts の outputFileTracingIncludes が配備で効くか**(Action が実行時に index.json を読むため) |
| 6〜10 | 未着手 | 手を付ける前に、各段の「先に決める判断」(下の表)を確認する |

## 順序の根拠

- トップを 3 番目にするのは、トップが「カード」「入口」「お知らせ」を全部使うため。先に作るとカードの形が決まった時に作り直しになる
- 一覧を詳細より先にするのは、カードの情報順(03 §6)が詳細の見出しの順にも効くため
- 条件固定の一覧(4)は一覧(1)の部品をそのまま使うので、1 が固まってからまとめて
- フォーム(5)は内見予約が詳細(2)から物件を引き継ぐので 2 の後。3本は別実装(J-007)で、1本目の手戻りを 2本目・3本目で測る(章3)
- 会社案内系(6)を後ろにするのは、文言が架空で判断が多く、部品への依存が少ないため
- noindex(8)は最後。先に入れると実機確認でクロールの動作が見えない

## 各段で先に決める判断(01 の判断ポイントから。実装前に SHIN が決めるもの)

| 順 | 先に決めること |
|---|---|
| 0 | 固定CTAの順序 / 架空注記との重なり(03 §9-2・9-3) |
| 1 | スマホの絞り込み方式(03 §9-1)/ 検索結果ページの処理方式(rules/static-rendering.md §4 の3案) |
| 2 | 地図ライブラリ(Leaflet / MapLibre)/ 成約済み詳細の扱い(03 §9-7)/ 物件情報の引き継ぎ方法(URL パラメータ) |
| 3 | 強み3点の文言 / お客様の声の件数と架空表記 |
| 4 | 0件時の緩和順序(03 §9-6) |
| 5 | **決定(J-102・SHIN)**:a 1本目の入口は `/contact` 1本・種別は `?kind=`・`/reserve` は廃止 / b スパム対策は Cloudflare Turnstile。トークンが無い・検証に失敗した送信は拒否 / c 確認画面は Server Action が返す(検証 → 確認 / 送信 の2段・同一 URL) / d 折り返しは一律「翌営業日まで」・営業時間等は site.ts・担当者名は出さず「賃貸部より」 / e 物件の引き継ぎは `?property=` のみ(ブラウザ側に持たない) / f `/privacy` を実装順 7 から前倒し / g `RealEstateListing` に `potentialAction` を追加・成約済みには出さない |
| 6 | BtoB 切替の範囲(03 §9-4)/ スタッフ紹介に写真を置くか(プレースホルダー) |
| 8 | 記録ページを index にするか |

## 変更履歴

| 版 | 日付 | 変更 | J-ID |
|---|---|---|---|
| v0.6 | 09/16 | 到達点の 5 に J-103 と残り3件(Turnstile の検証・Resend の到達確認・outputFileTracingIncludes)を明記 | J-102・J-103 |
| v0.5 | 09/15 | 実装順 5 を着手中に。「先に決める判断」の 5 を SHIN の決定 a〜g に置き換え。到達点に 5 の内容を追記 | J-102 |
| v0.4 | 09/15 | 実装順 4 を済に(条件固定38ページ+入口3ページ)。到達点と残り(地図モード・緩和順序の未決)を追記 | J-095 |
| v0.3 | 09/14 | 実装順 5 に potentialAction の保留、8 に詳細ページ分の先行実装を注記 | J-083 |
| v0.2 | 09/13 | 状態の列と「各段の到達点」を追加(0〜3 が済・4 以降は未着手) | — |
| v0.1 | 09/08 | 初案をそのまま採用 | — |
