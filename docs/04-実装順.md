# 04 実装順(v0.1 / 2026-09-08・そのまま採用)

原則:**共通部品を先に**(後で全ページが変わらないように)/ **データを使うページを先に**(型と JSON の不備を早く見つける)/ **同じ部品を使うページはまとめて**。
各段の完了条件は共通:スマホ 390px で崩れない / `DATA_SOURCE=static pnpm build` で全ルート Static / 判断ポイントに J-ID か「そのまま採用」/ before・after のスクショ。
手直しの扱いは `docs/03-デザイン基準.md` §10。

| 順 | 作るもの | 先に要るもの | ここで確定する部品 | 章 | 目安 |
|---|---|---|---|---|---|
| 0 | **土台**:03 のトークンを Tailwind 設定に / ヘッダー / フッター(J-027)/ 架空注記バー / スマホ固定CTA / ページ枠 | 03 の採否 | 全ページの外枠 | 2 | 3h |
| 1 | **物件カード + 物件一覧(賃貸)**:カード・バッジ・件数・並び替え・絞り込み(スマホ先行)・URL 同期 | 0・data 層 | カード、バッジ、絞り込みUI、URL 同期 | 1・2 | 6h |
| 2 | **物件詳細**:ギャラリー・価格・CTA・基本情報表・コメント・地図・関連4件・最近見た物件 | 1・地図ライブラリの選択(J-ID) | 表、地図、固定CTA の実動 | 1 | 6h |
| 3 | **トップ**:FV(決定済み)・探し方の入口・新着(カード再利用)・強み3点・3カード・お客様の声・お知らせ・店舗 | 0・1 | 検索フォーム → 一覧への遷移 | 1・2 | 4h |
| 4 | **売買一覧 + 条件固定の一覧**(エリア13 / 沿線6 / 駅13 / 特集6) | 1 | generateStaticParams、0件画面(F-001 の対処) | 4 | 3h |
| 5 | **フォーム3本**(内見予約 → 査定依頼 → オーナー相談。1本ずつ・別実装) | 2(内見は物件を引き継ぐ)・Resend の設定 | 入力→確認→完了、送信テスト、迷惑メール判定 | 3 | 9h |
| 6 | **会社案内系**:会社概要・スタッフ・店舗案内・初めての方へ・売却・オーナー(BtoB 切替)・お客様の声 | 0 | 文言(架空)、BtoB のトーン | 2 | 6h |
| 7 | **お知らせ**(一覧・詳細)・プライバシーポリシー・宅建業法表示 | 0 | 静的ページの型 | — | 2h |
| 8 | **SEO / AI 検索対応**:構造化データ(Organization / RealEstateListing / FAQ / Breadcrumb)・sitemap・llms.txt・OGP → その上で **noindex**(J-011) | 全ページ | — | 4 | 3h |
| 9 | **公開**:Vercel・環境変数・Lighthouse 前後・実機確認(端末数を記録) | 8 | 実測 | 4 | 4h |
| 10 | **記録ページ**(`/record`)・ルートの `README.md` | 記録シート | — | 6 | 3h |

目安の合計:約 49h(AI の見積もり。実測で置き換える)。累計 16h(09/08 時点)と足して約 65h。上限 100h・目標 80h の内側。

## 順序の根拠

- トップを 3 番目にするのは、トップが「カード」「入口」「お知らせ」を全部使うため。先に作るとカードの形が決まった時に作り直しになる
- 一覧を詳細より先にするのは、カードの情報順(03 §6)が詳細の見出しの順にも効くため
- 条件固定の一覧(4)は一覧(1)の部品をそのまま使うので、1 が固まってからまとめて
- フォーム(5)は内見予約が詳細(2)から物件を引き継ぐので 2 の後。3本は別実装(J-007)で、1本目の手戻りを 2本目・3本目で測る(章3)
- 会社案内系(6)を後ろにするのは、文言が架空で判断が多く、部品への依存が少ないため
- noindex(8)は最後。先に入れると実機確認でクロールの動作が見えない

## 各段で先に決める判断(01 の判断ポイントから。実装前に SHIN が決めるもの)

| 順 | 先に決めること |
|---|---|
| 0 | 固定CTAの順序 / 架空注記との重なり(03 §9-2・9-3) |
| 1 | スマホの絞り込み方式(03 §9-1)/ 検索結果ページの処理方式(rules/static-rendering.md §4 の3案) |
| 2 | 地図ライブラリ(Leaflet / MapLibre)/ 成約済み詳細の扱い(03 §9-7)/ 物件情報の引き継ぎ方法(URL パラメータ) |
| 3 | 強み3点の文言 / お客様の声の件数と架空表記 |
| 4 | 0件時の緩和順序(03 §9-6) |
| 5 | Turnstile を入れるか / 確認画面の有無 / 完了画面の折り返し目安 |
| 6 | BtoB 切替の範囲(03 §9-4)/ スタッフ紹介に写真を置くか(プレースホルダー) |
| 8 | 記録ページを index にするか |

## 変更履歴

| 版 | 日付 | 変更 | J-ID |
|---|---|---|---|
| v0.1 | 09/08 | 初案をそのまま採用 | — |
